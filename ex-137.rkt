#lang htdp/bsl


;;; Compare the template for `contains-flatt?` with the one for `how-many`. Ignoring the
;;; function name, they are the same. Explain the similarity.

;;; A:
;;;
;;; The templates are the same because they deal with the same type of data:
;;; a List-of-strings.
